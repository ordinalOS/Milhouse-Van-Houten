# millhouse

This is a tiny alias package for `milhouse`.

It exists so users who type the common misspelling can still install and run the CLI:

```bash
npm install -g millhouse
millhouse ui
```

The actual implementation lives in the `milhouse` package.
